# 整合回報：第 01 批子 Chat 輸出

## 【完成項目】

### 修改 1：插入 QA_EVENT_ZH_MAP 常數
- 定位：`    return t.strip()` 結束後（原第 751 行）、`# ── 手動覆蓋翻譯（image_file → 正確的 skill_zh）` 之前
- 內容：從 output_01.md 的【QA_EVENT_ZH_MAP 內容】完整插入（含 20 個事件牌名對照、共 26 個 key-value、行內注釋）
- 結果：插入成功，無語法錯誤

### 修改 2：更新 restore_event_names() 函式
- 定位：build_data.py 第 97～103 行（原函式）
- 變更：在 `for zh, jp in zh_to_jp.items():` 迴圈之後，`return text` 之前，插入補救層迴圈 `for zh, jp in QA_EVENT_ZH_MAP.items():`
- 結果：替換成功，函式現為 9 行（含 docstring）

---

## 【結果】

成功

---

## 【驗證】

**build_data.py 執行結果：**
```
cards_data.js  : 547 KB
cards_zh.json  : 671 KB
總卡片數        : 364 張
```
- 輸出「[OK] 生成完成，364張」✓

**HV-P01-056 id=261 的 question 欄位實際內容：**
```
如果我的對手打出HV-P01-093"誰だろうと受けて立つ"，並且我想在下一回合中當一個角色獲得+1到一個參數時使用該角色的技能，我可以在我的手上放置一張可以在事件區域中使用[=投擲]或[=攻擊]打出的事件卡嗎？
```
- 包含 `誰だろうと受けて立つ`（日文）✓
- 不含 `無論是誰我都會接受`（中文翻譯已還原）✓

---

## 【遺留問題】

繼承自 output_01.md，共 4 項：

1. **`音駒の専売特許じゃねえぞォ！` 的 JP 文字差異**：Q&A 日文中使用 `じゃねぇぞ`（小字ぇ），牌名為 `じゃねえぞ`（大字え），資料來源本身的不一致，非此次任務範圍，建議中樞記錄備查。

2. **`超インナークロス!!!` 的 zero-width space 問題**：`qa_data_zh.json` 中 HV-P02-007/351A 的翻譯文字含 U+200B，`QA_EVENT_ZH_MAP` 已納入此特殊字元版本。未來若重新翻譯 Q&A 資料仍可能再次出現。

3. **AI 翻譯一致性**：`どん ぴしゃり` 在不同 Q&A 條目被翻成 `Don Slap` 或 `Don Smack`，`QA_EVENT_ZH_MAP` 已涵蓋兩個變體，未來若有其他翻譯變體需再補充。

4. **掃描方式的侷限**：以 `「事件牌名」` marker 識別受影響條目，對 JP 文字本身牌名拼寫與 `all_cards.json` 不一致的情況可能有漏網之魚。

---

## 【待中樞決策】

無
